version: '2'

services:
  aiida-optimade:
    restart: always
    build:
      context: ..
      dockerfile: Dockerfile
    environment:
      AIIDA_PATH: /app
      AIIDA_PROFILE: quicksetup
    volumes:
     - '/tmp:/app/.aiida/repository-quicksetup'
     - '.:/profiles'
    ports:
      - '3253:80'
